---
- hosts: command
  vars:
    ansible_sudo_pass: "{{ lookup('passwordstore', inventory_hostname) }}"
  user: astra
  roles:
    - command
    - common
    - nfs-command
  
- hosts: dragons
  vars:
    ansible_sudo_pass: "{{ lookup('passwordstore', inventory_hostname) }}"
  user: dragon
  roles:
    - common

- hosts: database
  user: dragon
  roles:
    - database

- hosts: nfs
  user: dragon
  roles:
    - nfs

#- hosts: monitoring_server 
#  user: pi
#  roles:
#    - monitoring_server 
