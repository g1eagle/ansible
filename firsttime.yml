---
- hosts: command
  ansible_sudo_pass: "{{ lookup('password', 'passwords/' + inventory_hostname) }}"
  user: astra
  roles:
    - command
    - common
  
- hosts: dragons
  user: dragon
  roles:
    - common

#- hosts: monitoring_server 
#  user: pi
#  roles:
#    - monitoring_server 
